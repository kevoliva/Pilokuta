{% extends 'base.html.twig' %}

{% block title %}Creneau{% endblock %}

{% block body %}
    <h1>Creneau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ creneau.id }}</td>
            </tr>
            <tr>
                <th>LaDate</th>
                <td>{{ creneau.laDate ? creneau.laDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>HeureDebut</th>
                <td>{{ creneau.heureDebut }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ creneau.duree }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ creneau.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('creneau_index') }}">back to list</a>

    <a href="{{ path('creneau_edit', {'id': creneau.id}) }}">edit</a>

    {{ include('creneau/_delete_form.html.twig') }}
{% endblock %}
