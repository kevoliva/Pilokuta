{% extends 'base.html.twig' %}

{% block title %}Poule{% endblock %}

{% block body %}
    <h1>Poule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poule.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ poule.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('poule_index') }}">back to list</a>

    <a href="{{ path('modify_poule', {'id': poule.id}) }}">edit</a>

    {{ include('poule/_delete_form.html.twig') }}
{% endblock %}
