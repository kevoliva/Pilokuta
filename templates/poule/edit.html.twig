{% extends 'base.html.twig' %}

{% block title %}Edit Poule{% endblock %}

{% block body %}
    <h1>Edit Poule</h1>

    {{ include('poule/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('poule_index') }}">back to list</a>

    {{ include('poule/_delete_form.html.twig') }}
{% endblock %}
